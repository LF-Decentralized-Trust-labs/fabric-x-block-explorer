# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
name: Release Fabric-X Block Explorer Docker Image

on:
  push:
    tags:
      - "v*"  # Trigger on tags like v0.0.1

permissions:
  contents: read
  packages: write

jobs:
  docker-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up QEMU (for cross-platform builds)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772  # v3.4.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build release binaries (linux/amd64, linux/arm64)
        run: make build-release

      - name: Resolve version and image tags
        id: meta
        run: |
          # Normalise to lowercase — GHCR requires lowercase owner/repo paths.
          GHCR_REPO="ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')"
          echo "GHCR_REPO=${GHCR_REPO}" >> "$GITHUB_ENV"
          echo "CREATED=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_ENV"
          echo "REVISION=$(git rev-parse HEAD)" >> "$GITHUB_ENV"

          if [[ "${GITHUB_REF}" =~ ^refs/tags/v([0-9]+\.[0-9]+\.[0-9]+.*)$ ]]; then
            # ── Tagged release ────────────────────────────────────────────────
            # Strip the leading "v" from the git tag — this is the canonical version.
            VERSION="${BASH_REMATCH[1]}"
            echo "✅ Tag-derived version: ${VERSION}"
          else
            # ── Non-tag push (e.g. branch) ────────────────────────────────────
            VERSION="$(git describe --tags --always --dirty | sed 's/^v//')"
            echo "✅ git-describe version: ${VERSION}"
          fi

          # Both triggers publish :<version> and :latest.
          echo "VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "TAGS=${GHCR_REPO}:${VERSION},${GHCR_REPO}:latest" >> "$GITHUB_ENV"

      - name: Pin docker-compose.yaml image tags to release version
        run: |
          sed -i "s|:latest\b|:${VERSION}|g" docker-compose.yaml
          echo "✅ docker-compose.yaml pinned to ${VERSION}"

      - name: Build and push multi-platform Docker image (backend + UI)
        uses: docker/build-push-action@14487ce63c7a62a4a324b0bfb37086795e31c6c1  # v6.16.0
        with:
          context: .
          file: ./docker/images/combined/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.TAGS }}
          build-args: |
            VERSION=${{ env.VERSION }}
            CREATED=${{ env.CREATED }}
            REVISION=${{ env.REVISION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
